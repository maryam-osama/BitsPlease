<h1>Request</h1>
<%= form_for :request, url: requests_path do |f| %>
  
 
  <p>
    <%= f.label :reason %><br>
    <%= f.text_area :reason %>
    <%= f.text_area :user_email, :value => current_user.email %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>

<% end %>